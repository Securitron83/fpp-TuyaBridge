# Compiled output
*.so
*.o
src/*.o

# Backup files created before the C++ rewrite
*.bak

# Device config — contains local keys, never commit
devices.conf

# Debug mode flag — runtime state, not source
debug.flag

# Legacy Node.js files (replaced by C++ plugin)
tuya-mqtt.js
config.js
plugin_setup.old

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*~
